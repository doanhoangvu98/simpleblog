<h1><%= @post.title %></h1>
<p><%= @post.body %></p>
<h1>
<%= link_to "Edit", edit_post_path(@post), class: "btn btn-info" %>
<%= link_to "Delete", post_path(@post), method: :delete, data: {confirm: "Are you sure delete?"}, class: "btn btn-danger" %>
